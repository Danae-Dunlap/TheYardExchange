import {beforeAll, expect, test} from '@jest/globals';
import { fetchBusiness } from "./utils";

//Configure mock database connection here
beforeAll(() => {});

test('fetchBusiness returns correct business data', async () => {});
test('fetchBusiness handles errors gracefully', async () => {});


